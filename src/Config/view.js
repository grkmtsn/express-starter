module.exports = {
    /*
     * Express view engine. You can also use other engines that are compatible with express framework
     * like 'jade', 'mustache'
     */
    engine: 'pug',

    /*
     * View engine's render path
     * '../' means root of src
     */
    path: '../Views'
}